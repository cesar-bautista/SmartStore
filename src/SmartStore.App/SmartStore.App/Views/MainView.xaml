<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  mc:Ignorable="d"
                  x:Class="SmartStore.App.Views.MainView"
                  xmlns:views="clr-namespace:SmartStore.App.Views;assembly=SmartStore.App"
                  MasterBehavior="Popover"
                  Title="SmartStore">

    <!--<MasterDetailPage.ToolbarItems>
        <ToolbarItem
            Text="Search">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="Android, iOS" Value="netcar_search"/>
                    <On Platform="UWP" Value="Assets/netcar-search.png"/>
                </OnPlatform>
            </ToolbarItem.Icon>
        </ToolbarItem>
    </MasterDetailPage.ToolbarItems>-->
    <MasterDetailPage.Master>
        <views:MenuView BindingContext="{Binding MenuViewModel}"/>
    </MasterDetailPage.Master>
    <MasterDetailPage.Detail>
        <ContentPage 
            BackgroundColor="Transparent" />
    </MasterDetailPage.Detail>

</MasterDetailPage>
